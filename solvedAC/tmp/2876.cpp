#include<iostream>
using namespace std;

int main(void){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cin.tie(NULL);
    
    int a,b,v, sum=0;
    cin>>a>>b>>v;

    while(1){
        if
        sum+=a;
        sum-=b;
    }
}