#include<iostream>
#include<vector>
using namespace std;

int main(void) {
	vector<int> hotel;
	int h, w, n, t;

	cin >> t;
	for (int i = 0; i < t; i++)
		cin >> h >> w;

	for (int i = 1; i <= h; i++) {
		hotel[i] = (100 * i) + 1;
		if (hotel[i] == h * (100 * i) + 1) {
			hotel[i]=
		}
	}
}